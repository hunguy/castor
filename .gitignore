.claude

.debug/

.DS_Store

# Personal config override (keeps private sources/paths out of version control)
config.local.yaml
config.yaml

# Built castor binary (produced by `make build`)
/castor

# whisper.cpp cmake artifacts (produced by `make whisper-lib` or CD pipeline)
third_party/whisper.cpp/build_go/
third_party/whisper.cpp/build_*/

# zig cache (created by mlugg/setup-zig in CI)
.zig-cache/

# castor-api local artifacts
castor-api/.env
castor-api/.venv/
castor-api/.ropeproject/
castor-api/__pycache__/
